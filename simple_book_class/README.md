1. 定义一个 “图书（Book）” 类，包含以下属性：书名（name）、作者（author）、页数（pages，整数）；
2. 类中包含 2 个方法：
  - show_details()：打印图书完整信息（格式："书名：xxx，作者：xxx，页数：xxx页"）；
  - is_thick()：判断是否为 “厚书”（如果页数≥500，返回True，否则返回False）；
3. 创建 2 个图书对象（例如：《Python 编程》作者 “张三”，300 页；《C++ 实战》作者 “李四”，600 页）；
4. 调用对象的方法，分别打印两本书的信息，并判断是否为厚书。